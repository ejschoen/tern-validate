(ns leiningen.tern-validate
  "Inject the expected database version (from lein tern) into the project manifest."
  )

(defn tern-validate
  "Inject the expected database version (from lein tern) into the project manifest."
  [project & keys]
  project
  )
